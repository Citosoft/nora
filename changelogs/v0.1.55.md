### Improvements

- Nora now always sends a single anonymous app launch analytics event with coarse app, platform, and locale context for active user counts while keeping detailed usage analytics opt-in.

### Fixes

- Analytics launch tracking now passes typed event properties cleanly during production builds.
- Local agent terminals now stay open in an interactive shell after any agent command exits, instead of closing the PTY when the agent finishes.
