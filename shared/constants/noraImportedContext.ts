/**
 * Handoff bundles copied into the agent checkout (posix-style relative path).
 * Must stay in sync with main-process import logic.
 */
export const NORA_IMPORTED_CONTEXT_RELATIVE = ".nora/imported_context";
